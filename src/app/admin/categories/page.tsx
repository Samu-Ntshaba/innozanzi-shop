import { AdminPage, Panel, inputClass, buttonClass, tableClass } from "@/components/admin/admin-ui"; import { createCategory } from "@/domain/admin/actions"; import { getAdminCategories } from "@/domain/admin/queries"; import { requirePermission } from "@/domain/auth/session";
export default async function Page(){await requirePermission("products.update");const rows=await getAdminCategories();return <AdminPage title="Categories" description="Organise catalogue navigation." actions={<form action={createCategory} className="flex gap-2"><input className={inputClass} name="name" placeholder="Category name" required/><button className={buttonClass}>Add category</button></form>}><Panel><table className={tableClass}><thead><tr><th>Name</th><th>Slug</th><th>Products</th><th>Active</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.slug}</td><td>{x._count.products}</td><td>{x.isActive?"Yes":"No"}</td></tr>)}</tbody></table></Panel></AdminPage>}
