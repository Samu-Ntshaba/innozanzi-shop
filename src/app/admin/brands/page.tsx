import { AdminPage, Panel, inputClass, buttonClass, tableClass } from "@/components/admin/admin-ui"; import { createBrand } from "@/domain/admin/actions"; import { getAdminBrands } from "@/domain/admin/queries"; import { requirePermission } from "@/domain/auth/session";
export default async function Page(){await requirePermission("products.update");const rows=await getAdminBrands();return <AdminPage title="Brands" description="Manage product manufacturers and brands." actions={<form action={createBrand} className="flex flex-wrap gap-2"><input className={inputClass} name="name" placeholder="Brand name" required/><input className={inputClass} name="website" placeholder="https://…"/><button className={buttonClass}>Add brand</button></form>}><Panel><table className={tableClass}><thead><tr><th>Name</th><th>Website</th><th>Products</th><th>Active</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.website??"—"}</td><td>{x._count.products}</td><td>{x.isActive?"Yes":"No"}</td></tr>)}</tbody></table></Panel></AdminPage>}
