import Link from"next/link";import{prisma}from"@/lib/prisma";import{requirePermission}from"@/domain/auth/session";import{AdminPage,Panel,StatusBadge,tableClass}from"@/components/admin/admin-ui";
export default async function PartnerRequests(){await requirePermission("partnership.request.view");const rows=await prisma.partnerRequest.findMany({include:{partnership:{include:{owner:{select:{name:true,email:true}},partnershipType:true}},_count:{select:{items:true,offers:true}}},orderBy:[{urgency:"desc"},{createdAt:"desc"}],take:200});return <AdminPage title="Partner requests" description="Assign, source, propose alternatives and issue controlled commercial offers."><Panel className="p-0"><table className={tableClass}><thead><tr><th>Reference</th><th>Partner / track</th><th>Request</th><th>Urgency</th><th>Offers</th><th>Status</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><Link className="font-bold text-sky-700" href={`/admin/partnerships/requests/${r.id}`}>{r.requestNumber}</Link><br/><span className="text-xs text-slate-500">{r.createdAt.toLocaleDateString("en-ZA")}</span></td><td>{r.partnership.owner.name??r.partnership.owner.email}<br/><span className="text-xs">{r.partnership.partnershipType.name}</span></td><td>{r.title}<br/><span className="text-xs">{r._count.items} item(s)</span></td><td>{r.urgency}</td><td>{r._count.offers}</td><td><StatusBadge value={r.status}/></td></tr>)}</tbody></table></Panel></AdminPage>}
