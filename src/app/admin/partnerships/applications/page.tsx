import Link from"next/link";import{prisma}from"@/lib/prisma";import{requirePermission}from"@/domain/auth/session";import{AdminPage,Panel,StatusBadge,tableClass}from"@/components/admin/admin-ui";
export default async function Applications(){await requirePermission("partnership.view");const rows=await prisma.partnershipApplication.findMany({include:{applicant:{select:{name:true,email:true}},partnershipType:true,_count:{select:{documents:true}}},orderBy:{createdAt:"desc"},take:200});return <AdminPage title="Partnership applications" description="Review client eligibility, structured application sections and private evidence."><Panel className="p-0"><table className={tableClass}><thead><tr><th>Reference</th><th>Applicant</th><th>Track</th><th>Documents</th><th>Status</th><th>Review</th></tr></thead><tbody>{rows.map(a=><tr key={a.id}><td><strong>{a.applicationNumber}</strong><br/><span className="text-xs text-slate-500">{a.createdAt.toLocaleDateString("en-ZA")}</span></td><td>{a.applicant.name}<br/><span className="text-xs text-slate-500">{a.applicant.email}</span></td><td>{a.partnershipType.name}</td><td>{a._count.documents}</td><td><StatusBadge value={a.status}/></td><td><Link className="font-semibold text-sky-700" href={`/admin/partnerships/applications/${a.id}`}>Open review →</Link></td></tr>)}</tbody></table></Panel></AdminPage>}
