import Link from"next/link";import{prisma}from"@/lib/prisma";import{requirePermission}from"@/domain/auth/session";import{AdminPage,Panel,StatusBadge,tableClass}from"@/components/admin/admin-ui";
export default async function Partners(){await requirePermission("partnership.view");const rows=await prisma.partnership.findMany({include:{owner:{select:{name:true,email:true}},partnershipType:true,accountManager:{select:{name:true}},_count:{select:{requests:true}},reviews:{where:{completedAt:null},orderBy:{dueAt:"asc"},take:1}},orderBy:{approvedAt:"desc"}});return <AdminPage title="Approved partners" description="Relationship standing, reviews, commercial terms and access status."><Panel className="p-0"><table className={tableClass}><thead><tr><th>Partner</th><th>Track</th><th>Manager</th><th>Requests</th><th>Review</th><th>Status</th></tr></thead><tbody>{rows.map(p=><tr key={p.id}><td><Link className="font-bold text-sky-700" href={`/admin/partnerships/partners/${p.id}`}>{p.partnerNumber}</Link><br/><span className="text-xs">{p.owner.name??p.owner.email}</span></td><td>{p.partnershipType.name}</td><td>{p.accountManager?.name??"Unassigned"}</td><td>{p._count.requests}</td><td>{p.reviews[0]?.dueAt.toLocaleDateString("en-ZA")??"—"}</td><td><StatusBadge value={p.status}/></td></tr>)}</tbody></table></Panel></AdminPage>}
