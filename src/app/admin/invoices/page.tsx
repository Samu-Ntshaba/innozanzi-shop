import{AdminPage,Panel,StatusBadge,tableClass}from"@/components/admin/admin-ui";import{requirePermission}from"@/domain/auth/session";import{prisma}from"@/lib/prisma";
export default async function InvoicesPage({searchParams}:{searchParams:Promise<{created?:string}>}){await requirePermission("quotations.manage");const{created}=await searchParams;const rows=await prisma.invoice.findMany({include:{quotation:{select:{quotationNumber:true}}},orderBy:{createdAt:"desc"},take:100});const outstanding=rows.filter(i=>!["PAID","VOID"].includes(i.status)).reduce((n,i)=>n+Number(i.grandTotal),0);return <AdminPage title="Invoices" description="Customer billing documents created from accepted quotations.">{created?<div className="border border-emerald-300 bg-emerald-50 p-3 text-sm">Invoice <strong>{created}</strong> created.</div>:null}<div className="grid gap-3 sm:grid-cols-3"><Summary label="Documents" value={rows.length.toString()}/><Summary label="Outstanding" value={`R ${outstanding.toLocaleString("en-ZA",{minimumFractionDigits:2})}`}/><Summary label="Overdue" value={rows.filter(i=>i.status==="OVERDUE").length.toString()}/></div><Panel className="p-0"><table className={tableClass}><thead><tr><th>Invoice</th><th>Account</th><th>Source quote</th><th>Due</th><th>Total</th><th>Status</th></tr></thead><tbody>{rows.map(i=><tr key={i.id}><td><strong>{i.invoiceNumber}</strong></td><td>{i.companyName??i.customerName}<br/><span className="text-xs text-slate-500">{i.customerEmail}</span></td><td>{i.quotation?.quotationNumber??"Manual"}</td><td>{i.dueAt.toLocaleDateString("en-ZA")}</td><td className="font-semibold">R {Number(i.grandTotal).toLocaleString("en-ZA",{minimumFractionDigits:2})}</td><td><StatusBadge value={i.status}/></td></tr>)}{!rows.length?<tr><td colSpan={6} className="py-10 text-center text-slate-500">No invoices yet. Create one after a quote is accepted.</td></tr>:null}</tbody></table></Panel></AdminPage>}
function Summary({label,value}:{label:string;value:string}){return <div className="border border-slate-300 bg-white p-4"><p className="text-xs font-bold uppercase text-slate-500">{label}</p><p className="mt-1 text-2xl font-semibold">{value}</p></div>}
