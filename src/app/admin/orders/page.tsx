import { AdminPage, Panel, tableClass } from "@/components/admin/admin-ui"; import { setOrderStatus } from "@/domain/admin/actions"; import { getAdminOrders } from "@/domain/admin/queries"; import { requirePermission } from "@/domain/auth/session";
const statuses=["PENDING","AWAITING_PAYMENT","PAID","PROCESSING","READY_FOR_COLLECTION","SHIPPED","DELIVERED","COMPLETED","CANCELLED"];
export default async function Page(){await requirePermission("orders.view");const rows=await getAdminOrders();return <AdminPage title="Orders" description="Fulfilment and customer order status."><Panel><table className={tableClass}><thead><tr><th>Order</th><th>Customer</th><th>Total</th><th>Payment</th><th>Items</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td><strong>{x.orderNumber}</strong><br/><span className="text-xs text-slate-500">{x.createdAt.toLocaleDateString("en-ZA")}</span></td><td>{x.email}</td><td>R {x.grandTotal.toString()}</td><td>{x.paymentStatus}</td><td>{x._count.items}</td><td><form action={setOrderStatus} className="flex gap-2"><input type="hidden" name="id" value={x.id}/><select className="rounded border px-2 py-1" name="status" defaultValue={x.status}>{statuses.map(s=><option key={s}>{s}</option>)}</select><button className="text-sky-800 underline">Save</button></form></td></tr>)}</tbody></table></Panel></AdminPage>}
