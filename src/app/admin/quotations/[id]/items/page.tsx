import Link from"next/link";import{notFound}from"next/navigation";import{prisma}from"@/lib/prisma";import{requirePermission}from"@/domain/auth/session";import{addQuotationAlternative,removeQuotationItem}from"@/domain/quotations/editor-actions";import{AdminPage,Panel,buttonClass,inputClass,tableClass}from"@/components/admin/admin-ui";
export default async function ItemEditor({params}:{params:Promise<{id:string}>}){await requirePermission("quotations.manage");const{id}=await params;const[quote,products]=await Promise.all([prisma.quotation.findUnique({where:{id},include:{items:true}}),prisma.product.findMany({where:{status:"PUBLISHED",deletedAt:null},select:{id:true,name:true,sku:true,costPrice:true},orderBy:{name:"asc"},take:300})]);if(!quote)notFound();return <AdminPage title="Quotation item alternatives" description={`${quote.quotationNumber} · internal product replacement workspace`} actions={<Link className={buttonClass} href={`/admin/quotations/${id}`}>Return to final review</Link>}><Panel><p className="text-sm text-slate-600">Remove unavailable requested lines or add approved alternatives. Internal cost remains private and pricing is recalculated from the quotation markup.</p><table className={`${tableClass} mt-4`}><thead><tr><th>Item</th><th>Quantity</th><th>Action</th></tr></thead><tbody>{quote.items.map(item=><tr key={item.id}><td>{item.productName}<br/><span className="text-xs text-slate-500">{item.sku}</span></td><td>{item.quantity}</td><td>{quote.kind==="PROVISIONAL"?<form action={removeQuotationItem}><input type="hidden" name="quotationId" value={id}/><input type="hidden" name="itemId" value={item.id}/><button className="text-red-700 underline">Remove unavailable item</button></form>:"Final locked"}</td></tr>)}</tbody></table></Panel>{quote.kind==="PROVISIONAL"?<Panel><h2 className="font-bold">Add alternative product</h2><form action={addQuotationAlternative} className="mt-4 flex flex-wrap items-end gap-3"><input type="hidden" name="quotationId" value={id}/><label className="min-w-72 flex-1 text-sm">Product<select className={`${inputClass} mt-1 w-full`} name="productId" required><option value="">Select catalogue product</option>{products.map(p=><option key={p.id} value={p.id} disabled={!p.costPrice}>{p.name} · {p.sku}{!p.costPrice?" · cost missing":""}</option>)}</select></label><label className="text-sm">Quantity<input className={`${inputClass} mt-1 w-28`} name="quantity" type="number" min="1" defaultValue="1"/></label><button className={buttonClass}>Add alternative</button></form></Panel>:null}</AdminPage>}
