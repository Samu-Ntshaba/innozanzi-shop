import Link from"next/link";import{AdminPage,MetricCard,Panel}from"@/components/admin/admin-ui";import{getAdminDashboard}from"@/domain/admin/queries";import{requirePermission}from"@/domain/auth/session";
export default async function AdminDashboard(){await requirePermission("reports.view");const d=await getAdminDashboard();return <AdminPage title="Sales & service cockpit" description="Commercial pipeline, approvals and operational exceptions in one workspace." actions={<span className="border border-emerald-300 bg-emerald-50 px-3 py-2 text-xs font-bold text-emerald-800">● SYSTEM ONLINE</span>}><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Open requests" value={d.openRequests} detail="Require sales qualification"/><MetricCard label="Approval queue" value={d.quotesToApprove} detail="AI drafts awaiting a human"/><MetricCard label="Pipeline value" value={`R ${Number(d.pipeline).toLocaleString("en-ZA")}`} detail="Draft, issued and accepted"/><MetricCard label="Open invoices" value={d.outstandingInvoices} detail="Draft or awaiting settlement"/></div><div className="grid gap-4 xl:grid-cols-[1.5fr_1fr]"><Panel><div className="flex items-center justify-between border-b pb-3"><div><h2 className="font-semibold">Commercial work queue</h2><p className="text-xs text-slate-500">Prioritised actions for the sales team</p></div><Link className="text-sm font-semibold text-sky-700" href="/admin/quotations">Open pipeline →</Link></div><div className="divide-y"><Work label="Review AI quote drafts" value={d.quotesToApprove} href="/admin/quotations"/><Work label="Qualify customer requests" value={d.openRequests} href="/admin/quotations"/><Work label="Review payment proofs" value={d.pendingPayments} href="/admin/payments"/><Work label="Resolve low stock items" value={d.lowStock} href="/admin/inventory"/></div></Panel><Panel><h2 className="font-semibold">Business snapshot</h2><dl className="mt-4 grid grid-cols-2 gap-4"><Snap label="Paid revenue" value={`R ${Number(d.revenue).toLocaleString("en-ZA")}`}/><Snap label="Orders" value={d.orders}/><Snap label="Customers" value={d.customers}/><Snap label="Products" value={d.products}/></dl></Panel></div></AdminPage>}
function Work({label,value,href}:{label:string;value:number;href:string}){return <Link href={href} className="flex items-center justify-between py-4 hover:text-sky-700"><span className="text-sm font-medium">{label}</span><span className="bg-slate-100 px-2 py-1 text-xs font-bold">{value}</span></Link>}
function Snap({label,value}:{label:string;value:string|number}){return <div className="border-l-2 border-sky-600 pl-3"><dt className="text-xs text-slate-500">{label}</dt><dd className="mt-1 text-lg font-semibold">{value}</dd></div>}
