import{AdminPage,Panel,buttonClass,inputClass,tableClass}from"@/components/admin/admin-ui";import{requirePermission}from"@/domain/auth/session";import{saveTransportSetting}from"@/domain/logistics/actions";import{prisma}from"@/lib/prisma";
export default async function Settings(){await requirePermission("transport.settings.manage");const[categories,costs]=await Promise.all([prisma.transportCategory.findMany({orderBy:{displayOrder:"asc"}}),prisma.transportCostType.findMany({orderBy:{displayOrder:"asc"}})]);const box=(kind:"category"|"cost",rows:Array<{id:string;code:string;name:string;isActive:boolean}>)=><Panel title={kind==="category"?"Transport categories":"Cost component types"}><form action={saveTransportSetting} className="mb-4 flex flex-wrap gap-2"><input type="hidden" name="kind" value={kind}/><input className={`${inputClass} flex-1`} name="code" placeholder="Code" required/><input className={`${inputClass} flex-[2]`} name="name" placeholder="Display name" required/><button className={buttonClass}>Add</button></form><table className={tableClass}><thead><tr><th>Code</th><th>Name</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.code}</td><td>{x.name}</td><td>{x.isActive?"Active":"Inactive"}</td></tr>)}</tbody></table></Panel>;return <AdminPage title="Logistics configuration" description="Manage categories and cost types without hard-coding rates into transport workflows."><div className="grid gap-4 xl:grid-cols-2">{box("category",categories)}{box("cost",costs)}</div></AdminPage>}
