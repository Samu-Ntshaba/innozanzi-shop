import{AdminPage,Panel,buttonClass,inputClass,tableClass}from"@/components/admin/admin-ui";import{requirePermission}from"@/domain/auth/session";import{createTransportProvider}from"@/domain/logistics/actions";import{prisma}from"@/lib/prisma";
export default async function Providers(){await requirePermission("transport.providers.manage");const rows=await prisma.transportProvider.findMany({where:{deletedAt:null},orderBy:{name:"asc"}});return <AdminPage title="Transport providers" description="Courier, internal, technician, independent and supplier transport providers."><div className="grid gap-4 xl:grid-cols-[.7fr_1.3fr]"><Panel title="Add provider"><form action={createTransportProvider} className="grid gap-3"><input className={inputClass} name="name" placeholder="Provider name" required/><select className={inputClass} name="type">{["COURIER_COMPANY","LOGISTICS_COMPANY","SUPPLIER_DELIVERY","DISTRIBUTOR_DELIVERY","INTERNAL_DRIVER","INTERNAL_STAFF","TECHNICIAN","CUSTOMER_COLLECTION","INDEPENDENT_DRIVER","RIDE_HAILING","RENTAL_VEHICLE","OTHER"].map(x=><option key={x}>{x}</option>)}</select><input className={inputClass} name="contactPerson" placeholder="Contact person"/><input className={inputClass} name="phone" placeholder="Telephone"/><input className={inputClass} name="email" type="email" placeholder="Email"/><input className={inputClass} name="serviceAreas" placeholder="Service areas, comma separated"/><input className={inputClass} name="vehicleTypes" placeholder="Vehicle types, comma separated"/><textarea className={inputClass} name="paymentTerms" placeholder="Payment terms"/><button className={buttonClass}>Add provider</button></form></Panel><Panel className="p-0"><table className={tableClass}><thead><tr><th>Provider</th><th>Type</th><th>Contact</th><th>Areas</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td className="font-bold">{x.name}</td><td>{x.type.replaceAll("_"," ")}</td><td>{x.contactPerson??"—"}<small className="block">{x.phone??x.email}</small></td><td>{x.serviceAreas.join(", ")||"Not configured"}</td><td>{x.isActive?"Active":"Inactive"}</td></tr>)}</tbody></table></Panel></div></AdminPage>}
