import { AdminPage, Panel, tableClass } from "@/components/admin/admin-ui"; import { moderateReview } from "@/domain/admin/actions"; import { getAdminReviews } from "@/domain/admin/queries"; import { requirePermission } from "@/domain/auth/session";
export default async function Page(){await requirePermission("products.update");const rows=await getAdminReviews();return <AdminPage title="Reviews" description="Moderate customer reviews before publication."><Panel><table className={tableClass}><thead><tr><th>Product</th><th>Customer</th><th>Rating</th><th>Review</th><th>Status</th><th>Moderate</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.product.name}</td><td>{x.user.name??x.user.email}</td><td>{x.rating}/5</td><td className="max-w-xs truncate">{x.title??x.body??"—"}</td><td>{x.status}</td><td><form action={moderateReview} className="flex gap-2"><input type="hidden" name="id" value={x.id}/><button name="status" value="APPROVED" className="text-emerald-700 underline">Approve</button><button name="status" value="REJECTED" className="text-red-700 underline">Reject</button><button name="status" value="HIDDEN" className="text-slate-600 underline">Hide</button></form></td></tr>)}</tbody></table></Panel></AdminPage>}
