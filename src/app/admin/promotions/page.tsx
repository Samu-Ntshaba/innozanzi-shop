import { prisma } from "@/lib/prisma"; import { requirePermission } from "@/domain/auth/session"; import { createCoupon,toggleCoupon } from "@/domain/promotions/actions"; import { AdminPage,Panel,inputClass,buttonClass,tableClass } from "@/components/admin/admin-ui";
export default async function Page(){await requirePermission("settings.manage");const coupons=await prisma.coupon.findMany({include:{_count:{select:{redemptions:true}}},orderBy:{createdAt:"desc"}});return <AdminPage title="Promotions" description="Create controlled discounts with limits and validity windows."><Panel><form action={createCoupon} className="grid gap-3 md:grid-cols-4"><input className={inputClass} name="code" placeholder="Code" required/><select className={inputClass} name="type"><option>PERCENTAGE</option><option>FIXED</option></select><select className={inputClass} name="scope"><option>ORDER</option><option>PRODUCT</option><option>CATEGORY</option><option>CUSTOMER</option></select><input className={inputClass} name="value" type="number" step="0.01" placeholder="Value" required/><input className={inputClass} name="minimumOrderValue" type="number" step="0.01" placeholder="Minimum order"/><input className={inputClass} name="usageLimit" type="number" placeholder="Usage limit"/><input className={inputClass} name="startsAt" type="datetime-local"/><input className={inputClass} name="expiresAt" type="datetime-local"/><input className={`${inputClass} md:col-span-3`} name="description" placeholder="Description"/><button className={buttonClass}>Create coupon</button></form></Panel><Panel><table className={tableClass}><thead><tr><th>Code</th><th>Discount</th><th>Scope</th><th>Usage</th><th>Status</th><th>Action</th></tr></thead><tbody>{coupons.map(c=><tr key={c.id}><td><strong>{c.code}</strong></td><td>{c.type==="PERCENTAGE"?`${c.value}%`:`R ${c.value}`}</td><td>{c.scope}</td><td>{c._count.redemptions}{c.usageLimit?` / ${c.usageLimit}`:""}</td><td>{c.isActive?"Active":"Disabled"}</td><td><form action={toggleCoupon}><input type="hidden" name="id" value={c.id}/><button className="text-sky-800 underline">{c.isActive?"Disable":"Enable"}</button></form></td></tr>)}</tbody></table></Panel></AdminPage>}
