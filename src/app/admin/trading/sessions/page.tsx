import Link from"next/link";import{requirePermission}from"@/domain/auth/session";import{prisma}from"@/lib/prisma";import{createTradingSession}from"@/domain/trading/actions";import{AdminPage,Panel,StatusBadge,buttonClass,tableClass}from"@/components/admin/admin-ui";
export default async function Sessions(){await requirePermission("trading.market.manage");const rows=await prisma.tradingSession.findMany({include:{_count:{select:{jobs:true,decisions:true}}},orderBy:{startedAt:"desc"},take:100});return <AdminPage title="Trading sessions" description="Monthly and on-demand scans are checkpointed, leased and resumable."><Panel><form action={createTradingSession} className="flex gap-3"><select name="scanType" className="border px-3"><option value="ON_DEMAND">On-demand review</option><option value="MONTHLY">Current monthly review</option></select><button className={buttonClass}>Start or resume</button></form></Panel><Panel><table className={tableClass}><thead><tr><th>Session</th><th>Type</th><th>Status</th><th>Jobs</th><th>Recommendations</th><th>Started</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td><Link className="font-bold text-sky-700" href={`/admin/trading/sessions/${x.id}`}>{x.name}</Link></td><td>{x.scanType}</td><td><StatusBadge value={x.status}/></td><td>{x._count.jobs}</td><td>{x._count.decisions}</td><td>{x.startedAt.toLocaleString("en-ZA")}</td></tr>)}</tbody></table></Panel></AdminPage>}
