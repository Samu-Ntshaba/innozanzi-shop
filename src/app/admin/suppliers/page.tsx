import { AdminPage, Panel, inputClass, buttonClass, tableClass } from "@/components/admin/admin-ui"; import { createSupplier } from "@/domain/admin/actions"; import { getAdminSuppliers } from "@/domain/admin/queries"; import { requirePermission } from "@/domain/auth/session";
export default async function Page(){await requirePermission("products.update");const rows=await getAdminSuppliers();return <AdminPage title="Suppliers" description="Procurement contacts and linked products." actions={<form action={createSupplier} className="flex flex-wrap gap-2"><input className={inputClass} name="companyName" placeholder="Company" required/><input className={inputClass} name="email" type="email" placeholder="Email"/><input className={inputClass} name="phone" placeholder="Phone"/><button className={buttonClass}>Add supplier</button></form>}><Panel><table className={tableClass}><thead><tr><th>Company</th><th>Contact</th><th>Email</th><th>Products</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.companyName}</td><td>{x.contactPerson??x.phone??"—"}</td><td>{x.email??"—"}</td><td>{x._count.products}</td></tr>)}</tbody></table></Panel></AdminPage>}
