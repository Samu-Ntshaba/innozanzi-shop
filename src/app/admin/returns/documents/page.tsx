import Link from"next/link";import{AdminPage,Panel,StatusBadge,tableClass}from"@/components/admin/admin-ui";import{requirePermission}from"@/domain/auth/session";import{prisma}from"@/lib/prisma";
export default async function ReturnDocumentQueue(){await requirePermission("returns.documents.download");const rows=await prisma.returnCase.findMany({include:{customer:true,_count:{select:{inspections:true,resolutions:true,refunds:true}}},orderBy:{createdAt:"desc"},take:200});return <AdminPage title="Return documents" description="Case records, inspections, decisions, credit notes and refund confirmations."><Panel className="p-0"><table className={tableClass}><thead><tr><th>Case</th><th>Customer</th><th>Status</th><th>Available records</th><th></th></tr></thead><tbody>{rows.map(row=><tr key={row.id}><td><strong>{row.referenceNumber}</strong><small className="block">{row.productNameSnapshot}</small></td><td>{row.customer?.name??row.customer?.email??"Customer"}</td><td><StatusBadge value={row.status}/></td><td>{1+row._count.inspections+row._count.resolutions+row._count.refunds}</td><td><Link className="font-bold text-sky-700" href={`/admin/returns/${row.id}/documents`}>Open documents</Link></td></tr>)}</tbody></table></Panel></AdminPage>}
