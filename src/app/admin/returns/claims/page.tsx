import Link from"next/link";import{AdminPage,Panel,StatusBadge,tableClass}from"@/components/admin/admin-ui";import{requirePermission}from"@/domain/auth/session";import{prisma}from"@/lib/prisma";
export default async function Claims(){await requirePermission("returns.claims.manage");const rows=await prisma.distributorClaim.findMany({include:{supplier:true,returnCase:{include:{customer:true}}},orderBy:{createdAt:"desc"},take:200});return <AdminPage title="Distributor claims and recoveries" description="Supplier recovery remains separate from the customer resolution and original sale."><Panel className="p-0"><table className={tableClass}><thead><tr><th>Claim</th><th>Distributor</th><th>Case / product</th><th>Claimed</th><th>Recovered</th><th>Status</th></tr></thead><tbody>{rows.map(row=><tr key={row.id}><td><Link className="font-bold text-sky-700" href={`/admin/returns/claims/${row.id}`}>{row.claimNumber}</Link></td><td>{row.supplier?.companyName??"Unassigned"}</td><td>{row.returnCase.referenceNumber}<small className="block">{row.productName}</small></td><td>R {Number(row.claimAmount).toFixed(2)}</td><td>R {Number(row.receivedAmount).toFixed(2)}</td><td><StatusBadge value={row.status}/></td></tr>)}</tbody></table></Panel></AdminPage>}
