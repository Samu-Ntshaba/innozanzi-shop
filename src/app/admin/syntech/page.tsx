import {prisma}from"@/lib/prisma";import{requirePermission}from"@/domain/auth/session";import{runSyntechFullSync,runSyntechIncrementalSync,testSyntechConnection}from"@/domain/admin/syntech-actions";import{AdminPage,Panel,buttonClass,secondaryButtonClass,StatusBadge,tableClass}from"@/components/admin/admin-ui";
export default async function Page(){await requirePermission("products.update");const [feed,runs,count]=await Promise.all([prisma.supplierFeed.findFirst({where:{provider:"SYNTECH"},include:{supplier:true}}),prisma.supplierSyncRun.findMany({where:{feed:{provider:"SYNTECH"}},orderBy:{startedAt:"desc"},take:20}),prisma.supplierCatalogueProduct.count({where:{active:true,feed:{provider:"SYNTECH"}}})]);const configured=Boolean(process.env.SYNTECH_FULL_FEED_URL&&process.env.SYNTECH_UPDATE_FEED_URL),latest=runs[0],healthy=configured&&latest?.status==="SUCCEEDED"&&!feed?.lastError;return <AdminPage title="Supplier feed management" description="Monitor the daily Syntech catalogue, stock and price refresh."><div className="grid gap-5 lg:grid-cols-3"><Panel title="Supplier status"><p className="text-2xl font-bold">{configured?"Connected":"Configuration required"}</p><p className="mt-2 text-sm text-slate-600">{count.toLocaleString("en-ZA")} active cached products</p><form action={testSyntechConnection} className="mt-4"><button className={secondaryButtonClass}>Test configuration</button></form></Panel><Panel title="Daily synchronisation"><p className="text-sm text-slate-600">Last success: {feed?.lastSuccessAt?.toLocaleString("en-ZA")??"Never"}<br/>Next expected refresh: {feed?.nextSyncAt?.toLocaleString("en-ZA")??"After first sync"}</p><div className="mt-4 flex flex-wrap gap-2"><form action={runSyntechIncrementalSync}><button disabled={!configured} className={buttonClass}>Refresh prices now</button></form><form action={runSyntechFullSync}><button disabled={!configured} className={secondaryButtonClass}>Rebuild catalogue now</button></form></div></Panel><Panel title="Feed health"><p className="text-sm text-slate-600">Production performs one authoritative full refresh every day. Use the compact update feed only when prices or stock need another refresh during the day.</p>{healthy?<p className="mt-3 text-sm font-semibold text-emerald-700">Latest feed run succeeded</p>:<p className="mt-3 rounded-lg bg-rose-50 p-3 text-sm text-rose-800">{feed?.lastError??(configured?"A successful feed run has not been recorded.":"Feed credentials are not configured.")}</p>}</Panel></div><Panel title="Recent synchronisations"><div className="overflow-x-auto"><table className={tableClass}><thead><tr><th>Started</th><th>Mode</th><th>Status</th><th>Received</th><th>Added</th><th>Updated</th><th>Removed</th><th>Skipped</th></tr></thead><tbody>{runs.map(run=><tr key={run.id}><td>{run.startedAt.toLocaleString("en-ZA")}</td><td>{run.mode}</td><td><StatusBadge value={run.status}/></td><td>{run.recordsReceived}</td><td>{run.recordsAdded}</td><td>{run.recordsUpdated}</td><td>{run.recordsRemoved}</td><td>{run.recordsSkipped}</td></tr>)}</tbody></table>{!runs.length?<p className="py-8 text-center text-sm text-slate-500">No synchronisations have run.</p>:null}</div></Panel><Panel title="Production policy"><ul className="list-disc space-y-2 pl-5 text-sm text-slate-600"><li>Supplier cost and recommended retail values remain visible only to authorised staff.</li><li>The public price is calculated by Innozanzi and never exposes the supplier cost.</li><li>The storefront reads a local catalogue cache, so a supplier outage does not directly break browsing.</li><li>The daily full sync discovers new products and removes products absent from the authoritative feed.</li><li>Orders preserve the exact product, price and supplier references used at checkout.</li></ul></Panel></AdminPage>}
