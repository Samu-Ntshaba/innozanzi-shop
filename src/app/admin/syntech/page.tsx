import { prisma } from "@/lib/prisma"; import { requirePermission } from "@/domain/auth/session"; import { runSyntechSync } from "@/domain/admin/syntech-actions"; import { AdminPage,Panel,buttonClass } from "@/components/admin/admin-ui";
export default async function Page(){await requirePermission("products.update");const last=await prisma.auditLog.findFirst({where:{action:"supplier.syntech.sync"},orderBy:{createdAt:"desc"}});const configured=Boolean(process.env.SYNTECH_FEED_URL);return <AdminPage title="Syntech catalogue sync" description="Import the official reseller feed, map categories, copy images to storage and update internal availability."><div className="grid gap-5 lg:grid-cols-2"><Panel><h2 className="font-semibold">Official feed</h2><p className="mt-2 text-sm text-slate-600">{configured?"The Syntech reseller feed is configured. Syncs update existing SKUs and create missing products.":"Add SYNTECH_FEED_URL from your Syntech reseller account before running a sync."}</p><form action={runSyntechSync} className="mt-5"><button disabled={!configured} className={`${buttonClass} disabled:cursor-not-allowed disabled:opacity-50`}>Sync Syntech now</button></form></Panel><Panel><h2 className="font-semibold">Last sync</h2>{last?<><p className="mt-2 text-sm text-slate-600">{last.createdAt.toLocaleString("en-ZA")}</p><pre className="mt-4 overflow-auto rounded-lg bg-slate-100 p-3 text-xs">{JSON.stringify(last.metadata,null,2)}</pre></>:<p className="mt-2 text-sm text-slate-600">No imports have run yet.</p>}</Panel></div><Panel><h2 className="font-semibold">Import policy</h2><ul className="mt-3 list-disc space-y-2 pl-5 text-sm text-slate-600"><li>Uses Syntech&apos;s authorised reseller XML/CSV feed, not website scraping.</li><li>Exact stock quantities remain internal; storefront customers see availability.</li><li>Images are copied to your configured public Supabase bucket.</li><li>AI category matching is optional and falls back safely.</li><li>Retail pricing applies the configured markup and never exposes supplier cost.</li></ul></Panel></AdminPage>}
