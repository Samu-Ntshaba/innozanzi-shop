import { AdminPage,Panel,buttonClass,inputClass } from "@/components/admin/admin-ui";import { requirePermission } from "@/domain/auth/session";import { uploadMarketingMedia } from "@/domain/marketing/actions";import { prisma } from "@/lib/prisma";
export default async function Media(){await requirePermission("marketing.media.manage");const rows=await prisma.mediaAsset.findMany({where:{isTestData:false},orderBy:{createdAt:"desc"},take:100});return <AdminPage title="Media library" description="Public, stable marketing images with required alternative text and social-image classification."><Panel title="Upload image"><form action={uploadMarketingMedia} className="grid gap-3 md:grid-cols-2"><input className={inputClass} name="file" type="file" accept="image/jpeg,image/png,image/webp,image/svg+xml" required/><input className={inputClass} name="title" placeholder="Internal image title"/><input className={`${inputClass} md:col-span-2`} name="altText" placeholder="Describe the image for customers using assistive technology" required/><input className={inputClass} name="caption" placeholder="Optional caption"/><label className="flex items-center gap-2"><input name="isSocialImage" type="checkbox"/> Suitable for social sharing</label><button className={`${buttonClass} md:col-span-2`}>Upload public image</button></form></Panel><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{rows.map(x=><article className="border border-slate-300 bg-white p-3" key={x.id}><div className="aspect-video overflow-hidden bg-slate-100"><img className="h-full w-full object-contain" src={x.publicUrl} alt={x.altText}/></div><h2 className="mt-3 font-semibold">{x.title??x.altText}</h2><p className="mt-1 text-xs text-slate-500">{x.altText}</p><button className="mt-2 text-xs text-sky-700" type="button">{x.publicUrl}</button></article>)}</div></AdminPage>}
