import { AdminPage,Panel,buttonClass,inputClass,tableClass } from "@/components/admin/admin-ui";import { requirePermission } from "@/domain/auth/session";import { createRedirect,toggleRedirect } from "@/domain/marketing/actions";import { prisma } from "@/lib/prisma";
export default async function Redirects(){await requirePermission("marketing.redirects.manage");const rows=await prisma.redirectRule.findMany({orderBy:{updatedAt:"desc"},take:250});return <AdminPage title="Redirects" description="Preserve existing search results and shared links when public URLs change."><Panel title="Create or replace redirect"><form action={createRedirect} className="grid gap-3 md:grid-cols-[1fr_1fr_160px_auto]"><input className={inputClass} name="sourcePath" placeholder="/old-public-path" required/><input className={inputClass} name="targetPath" placeholder="/new-public-path" required/><select className={inputClass} name="type"><option value="PERMANENT">Permanent · 308</option><option value="TEMPORARY">Temporary · 307</option></select><button className={buttonClass}>Save redirect</button></form></Panel><Panel><table className={tableClass}><thead><tr><th>From</th><th>To</th><th>Type</th><th>Hits</th><th>Status</th><th></th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.sourcePath}</td><td>{x.targetPath}</td><td>{x.type}</td><td>{x.hitCount}</td><td>{x.isActive?"Active":"Disabled"}</td><td><form action={toggleRedirect}><input type="hidden" name="id" value={x.id}/><button className="font-semibold text-sky-700 underline">{x.isActive?"Disable":"Enable"}</button></form></td></tr>)}</tbody></table></Panel></AdminPage>}
