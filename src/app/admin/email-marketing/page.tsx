import{AdminPage,buttonClass,inputClass,Panel,StatusBadge,tableClass}from"@/components/admin/admin-ui";import{createCampaign,sendCampaign}from"@/domain/communications/actions";import{requirePermission}from"@/domain/auth/session";import{prisma}from"@/lib/prisma";
export default async function EmailMarketingPage(){await requirePermission("customers.manage");const[subscribers,campaigns]=await Promise.all([prisma.newsletterSubscriber.findMany({orderBy:{subscribedAt:"desc"},take:200}),prisma.emailCampaign.findMany({orderBy:{createdAt:"desc"},take:50})]);return <AdminPage title="Email marketing" description="Consent-based newsletter audience and branded campaigns."><div className="grid gap-4 xl:grid-cols-[.8fr_1.2fr]"><Panel><h2 className="font-semibold">Create campaign</h2><form action={createCampaign} className="mt-4 grid gap-3"><input className={inputClass} name="name" placeholder="Internal campaign name" required/><input className={inputClass} name="subject" placeholder="Email subject" required/><input className={inputClass} name="preview" placeholder="Inbox preview text"/><textarea className={`${inputClass} min-h-40`} name="html" placeholder="Campaign content (basic HTML supported)" required/><button className={buttonClass}>Save draft</button></form></Panel><Panel className="p-0"><table className={tableClass}><thead><tr><th>Campaign</th><th>Status</th><th>Audience</th><th>Action</th></tr></thead><tbody>{campaigns.map(c=><tr key={c.id}><td><strong>{c.name}</strong><br/><span className="text-xs text-slate-500">{c.subject}</span></td><td><StatusBadge value={c.status}/></td><td>{subscribers.filter(s=>s.isActive).length}</td><td>{c.status==="DRAFT"?<form action={sendCampaign}><input type="hidden" name="id" value={c.id}/><button className="font-semibold text-sky-700">Send campaign</button></form>:c.sentAt?.toLocaleDateString("en-ZA")}</td></tr>)}</tbody></table></Panel></div><Panel className="p-0"><table className={tableClass}><thead><tr><th>Subscriber</th><th>Source</th><th>Status</th><th>Subscribed</th></tr></thead><tbody>{subscribers.map(s=><tr key={s.id}><td>{s.name??"Subscriber"}<br/><span className="text-xs text-slate-500">{s.email}</span></td><td>{s.source}</td><td><StatusBadge value={s.isActive?"ACTIVE":"UNSUBSCRIBED"}/></td><td>{s.subscribedAt.toLocaleDateString("en-ZA")}</td></tr>)}</tbody></table></Panel></AdminPage>}
