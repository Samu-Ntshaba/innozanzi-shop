import { prisma } from "@/lib/prisma"; import { requirePermission } from "@/domain/auth/session"; import { AdminPage,Panel,tableClass } from "@/components/admin/admin-ui";
export default async function Page(){await requirePermission("users.manage");const rows=await prisma.auditLog.findMany({include:{actor:{select:{email:true}}},orderBy:{createdAt:"desc"},take:200});return <AdminPage title="Audit log" description="Immutable record of privileged and integration activity."><Panel><table className={tableClass}><thead><tr><th>Time</th><th>Actor</th><th>Action</th><th>Entity</th><th>ID</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.createdAt.toLocaleString("en-ZA")}</td><td>{r.actor?.email??"System"}</td><td>{r.action}</td><td>{r.entityType}</td><td className="font-mono text-xs">{r.entityId??"-"}</td></tr>)}</tbody></table></Panel></AdminPage>}
