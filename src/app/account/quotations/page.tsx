import Link from "next/link";import{prisma}from"@/lib/prisma";import{requireUser}from"@/domain/auth/session";import{submitPaymentProof}from"@/domain/payments/actions";import{formatZar}from"@/lib/money";
const input="mt-1 min-h-11 w-full rounded-lg border border-slate-300 px-3";
export default async function CustomerQuotations({searchParams}:{searchParams:Promise<{submitted?:string;payment?:string}>}){const ctx=await requireUser();const params=await searchParams;const quotes=await prisma.quotation.findMany({where:{customerId:ctx.user.id},include:{items:true,paymentSubmissions:{orderBy:{submittedAt:"desc"},take:1}},orderBy:{createdAt:"desc"}});return <main className="mx-auto max-w-6xl px-4 py-10"><div className="flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-3xl font-black">Your quotations</h1><p className="mt-2 text-slate-600">Follow provisional review, final quotation and payment verification progress.</p></div><Link className="rounded-lg bg-sky-600 px-5 py-3 font-bold text-white" href="/shop">Start another request</Link></div>{(params.submitted||params.payment)&&<p className="mt-5 rounded-lg bg-emerald-50 p-4 text-emerald-800">{params.payment?"Proof received. Payment verification is pending.":`Quotation request ${params.submitted} was submitted.`}</p>}<div className="mt-7 space-y-5">{quotes.map(q=>{const canUpload=q.kind==="FINAL"&&["FINAL_APPROVED","SENT","PAYMENT_REJECTED"].includes(q.status)&&q.validUntil>new Date();return <article className="rounded-xl border bg-white p-5 shadow-sm" key={q.id}><div className="flex flex-wrap justify-between gap-4"><div><p className="text-xs font-bold uppercase text-sky-700">{q.kind} quotation</p><h2 className="mt-1 text-xl font-bold">{q.quotationNumber} · v{q.version}</h2><p className="mt-1 text-sm text-slate-500">Expires {q.validUntil.toLocaleDateString("en-ZA")}</p></div><div className="text-right"><p className="rounded bg-slate-100 px-3 py-1 text-xs font-bold">{q.status.replaceAll("_"," ")}</p><p className="mt-2 text-xl font-black">{formatZar(q.grandTotal.toString())}</p></div></div><div className="mt-4 divide-y rounded-lg border">{q.items.map(i=><div className="flex justify-between gap-4 p-3 text-sm" key={i.id}><span>{i.productName} × {i.quantity}</span><strong>{formatZar(i.lineTotal.toString())}</strong></div>)}</div><div className="mt-4 flex flex-wrap gap-3"><Link className="font-semibold text-sky-700 underline" href={`/api/quotations/${q.quotationNumber}/pdf`} target="_blank">Open PDF</Link>{q.convertedOrderId?<Link className="font-semibold text-sky-700 underline" href={`/account/orders/${q.paymentSubmissions[0]?.orderId?"by-quotation?quotation="+q.quotationNumber:""}`}>Track active order</Link>:null}</div>{q.kind==="PROVISIONAL"?<p className="mt-4 rounded-lg bg-amber-50 p-3 text-sm text-amber-900">This is provisional. Do not pay until a final quotation is approved and sent.</p>:null}{canUpload?<form action={submitPaymentProof} className="mt-5 grid gap-3 rounded-lg bg-slate-50 p-4 sm:grid-cols-2"><input type="hidden" name="quotationId" value={q.id}/><h3 className="font-bold sm:col-span-2">Upload proof of payment</h3><label>Amount paid<input className={input} name="amount" type="number" min="0.01" step="0.01" defaultValue={q.grandTotal.toString()} required/></label><label>Payment date<input className={input} name="paymentDate" type="date" required/></label><label>Payment reference<input className={input} name="paymentReference" defaultValue={q.paymentReference??q.quotationNumber} required/></label><label>Proof document<input className={`${input} py-2`} name="proof" type="file" accept=".pdf,image/jpeg,image/png,image/webp" required/></label><label className="sm:col-span-2">Optional note<textarea className={`${input} min-h-20 py-2`} name="customerNote"/></label><p className="text-xs text-slate-500 sm:col-span-2">Uploading proof does not verify payment. Your order starts only after finance approval.</p><button className="rounded-lg bg-[#071b33] px-5 py-3 font-bold text-white sm:col-span-2">Submit proof for verification</button></form>:null}{q.paymentSubmissions[0]?<p className="mt-4 text-sm"><strong>Latest payment submission:</strong> {q.paymentSubmissions[0].status.replaceAll("_"," ")}{q.paymentSubmissions[0].rejectionReason?` — ${q.paymentSubmissions[0].rejectionReason}`:""}</p>:null}</article>})}{!quotes.length?<p className="rounded-xl border border-dashed p-10 text-center text-slate-500">No quotation requests yet.</p>:null}</div></main>}
