import Link from"next/link";import{prisma}from"@/lib/prisma";import{requireUser}from"@/domain/auth/session";
export default async function Requests(){const ctx=await requireUser();const partner=await prisma.partnership.findFirstOrThrow({where:{userId:ctx.user.id,status:{in:["APPROVED","CONDITIONALLY_APPROVED"]}}});const rows=await prisma.partnerRequest.findMany({where:{partnershipId:partner.id},include:{_count:{select:{offers:true}}},orderBy:{createdAt:"desc"}});return <main className="mx-auto max-w-6xl px-4 py-10"><div className="flex justify-between gap-4"><div><h1 className="text-3xl font-black">Partner requests</h1><p className="mt-2 text-slate-600">Sourcing, availability and procurement responses.</p></div><Link className="h-fit rounded-lg bg-sky-600 px-5 py-3 font-bold text-white" href="/account/partner/requests/new">New request</Link></div><div className="mt-7 overflow-x-auto rounded-xl border bg-white"><table className="w-full text-left text-sm"><thead className="bg-slate-50"><tr><th className="p-3">Reference</th><th>Request</th><th>Status</th><th>Offers</th><th>Date</th></tr></thead><tbody>{rows.map(r=><tr className="border-t" key={r.id}><td className="p-3"><Link className="font-bold text-sky-700" href={`/account/partner/requests/${r.id}`}>{r.requestNumber}</Link></td><td>{r.title}</td><td>{r.status.replaceAll("_"," ")}</td><td>{r._count.offers}</td><td>{r.createdAt.toLocaleDateString("en-ZA")}</td></tr>)}</tbody></table></div></main>}
