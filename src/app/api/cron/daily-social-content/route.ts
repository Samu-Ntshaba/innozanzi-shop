import { authorisedMarketingCron, generateDailySocialContent, socialSettings } from "@/domain/marketing/social-content";
export async function POST(request: Request) { if (!authorisedMarketingCron(request)) return Response.json({ error: "Unauthorized" }, { status: 401 }); const settings = await socialSettings(); const hour = Number(new Intl.DateTimeFormat("en-ZA", { timeZone: "Africa/Johannesburg", hour: "2-digit", hour12: false }).format(new Date())); if (hour < settings.generationHour) return Response.json({ status: "too-early", scheduledHour: settings.generationHour }); try { return Response.json(await generateDailySocialContent()); } catch (error) { console.error("Daily social content cron failed", error); return Response.json({ error: error instanceof Error ? error.message : "Generation failed" }, { status: 500 }); } }
