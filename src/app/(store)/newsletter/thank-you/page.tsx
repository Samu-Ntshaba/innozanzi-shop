import Link from "next/link";
import { CheckCircle2, CircleAlert, FlaskConical } from "lucide-react";
export default async function NewsletterThankYou({searchParams}:{searchParams:Promise<{delivery?:string}>}){const{delivery}=await searchParams;const sent=delivery==="sent";const sandbox=delivery==="sandbox";const successful=sent||sandbox;const Icon=sent?CheckCircle2:sandbox?FlaskConical:CircleAlert;const tone=sent?"bg-emerald-100 text-emerald-700":sandbox?"bg-amber-100 text-amber-700":"bg-red-100 text-red-700";return <main className="mx-auto grid min-h-[65vh] max-w-xl place-items-center px-4 py-16 text-center"><section className="w-full rounded-2xl border border-slate-200 bg-white p-8 shadow-sm sm:p-10"><span className={`mx-auto grid size-14 place-items-center rounded-full ${tone}`}><Icon className="size-7"/></span><h1 className="mt-5 text-3xl font-black text-slate-950">{sent?"You’re on the list":sandbox?"Test subscription captured":"We couldn’t subscribe you"}</h1><p className="mt-3 leading-7 text-slate-600">{sent?"Your welcome email has been sent. Watch your inbox for useful ICT offers, new arrivals and advice.":sandbox?"The welcome email was captured in Mailtrap Sandbox inbox 4808141. Sandbox messages are for development testing and are not delivered to your personal inbox.":"We could not send your confirmation email, so your details were not saved. Please try again after our email service has been restored."}</p><Link className="mt-7 inline-flex min-h-12 items-center rounded-lg bg-sky-600 px-6 font-bold text-white hover:bg-sky-700" href={successful?"/shop":"/#newsletter"}>{successful?"Continue shopping":"Try again"}</Link></section></main>}
