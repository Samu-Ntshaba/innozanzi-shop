import { submitQuotationRequest } from "@/domain/quotations/actions";

const input = "mt-1 min-h-12 w-full min-w-0 rounded-lg border border-slate-300 px-3 text-base";

export default async function Page({ searchParams }: { searchParams: Promise<{ submitted?: string }> }) {
  const { submitted } = await searchParams;
  return <main className="mx-auto max-w-2xl px-4 py-8 sm:px-6 sm:py-12"><h1 className="text-3xl font-bold text-[#071b33]">Request a quotation</h1><p className="mt-2 text-slate-600">Tell us what your organisation needs. Our sales team will prepare a formal quotation.</p>{submitted ? <div className="mt-8 break-words rounded-xl bg-emerald-50 p-5 text-emerald-900"><strong>Request received.</strong> Your reference is {submitted}.</div> : <form action={submitQuotationRequest} className="mt-7 grid gap-4 rounded-xl border bg-white p-4 shadow-sm sm:grid-cols-2 sm:p-6"><label>Name<input className={input} name="contactName" required /></label><label>Email<input className={input} name="email" type="email" required /></label><label>Phone<input className={input} name="phone" type="tel" /></label><label>Company<input className={input} name="companyName" /></label><label className="sm:col-span-2">Product or service<input className={input} name="productName" required /></label><label>Quantity<input className={input} name="quantity" type="number" min="1" inputMode="numeric" defaultValue="1" required /></label><label className="sm:col-span-2">Requirements<textarea className={input} name="requirements" rows={5} required /></label><button className="min-h-12 rounded-lg bg-[#071b33] px-5 py-3 font-semibold text-white sm:col-span-2">Submit request</button></form>}</main>;
}
