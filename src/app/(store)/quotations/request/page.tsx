import Link from"next/link";import type{Metadata}from"next";import{requireUser}from"@/domain/auth/session";import{getCurrentCart}from"@/domain/cart/service";import{submitCartQuotationRequest}from"@/domain/quotations/actions";
export const metadata:Metadata={title:"Submit quotation request",robots:{index:false,follow:false}};const input="mt-1 min-h-12 w-full rounded-md border border-slate-300 px-3";
export default async function Page(){const{user}=await requireUser();const cart=await getCurrentCart();const manual=cart?.items??[],supplier=cart?.supplierItems??[],empty=!manual.length&&!supplier.length;return <main className="mx-auto max-w-4xl px-4 py-10 sm:px-6"><p className="text-xs font-bold uppercase tracking-widest text-sky-700">Quotation request</p><h1 className="mt-2 text-3xl font-bold">Confirm requested products</h1><p className="mt-2 text-slate-600">This is a quotation request. No payment is collected and stock is rechecked by our team.</p>{empty?<div className="mt-8 rounded-xl border border-dashed p-10 text-center"><h2 className="text-xl font-semibold">Your quotation list is empty</h2><Link className="mt-5 inline-block rounded-lg bg-sky-700 px-5 py-3 font-bold text-white" href="/shop">Select products</Link></div>:<><section className="mt-7 rounded-xl border bg-white p-5"><h2 className="font-bold">Request summary</h2><p className="mt-2 text-sm text-slate-600">{manual.length} internal catalogue item{manual.length===1?"":"s"} · {supplier.length} supplier catalogue item{supplier.length===1?"":"s"}</p><Link className="mt-3 inline-block text-sm font-semibold text-sky-700 underline" href="/cart">Review products and quantities</Link></section><form action={submitCartQuotationRequest} className="mt-6 grid gap-4 rounded-xl border bg-white p-5 sm:grid-cols-2"><label>Contact name<input className={input} name="contactName" defaultValue={user.name??""} required/></label><label>Email<input className={`${input} bg-slate-50`} value={user.email} readOnly/></label><label>Phone<input className={input} name="phone" type="tel"/></label><label>Company<input className={input} name="companyName"/></label><label>VAT number<input className={input} name="vatNumber"/></label><label>Delivery required?<select className={input} name="deliveryRequired" defaultValue="yes"><option value="yes">Yes</option><option value="no">No / collection</option></select></label><label className="sm:col-span-2">Delivery address or instructions<textarea className={`${input} min-h-24 py-3`} name="deliveryAddress"/></label><label className="sm:col-span-2">Requirements or notes<textarea className={`${input} min-h-32 py-3`} name="customerNotes"/></label><button className="min-h-12 rounded-lg bg-[#071b33] px-5 py-3 font-semibold text-white sm:col-span-2">Submit quotation request</button></form></>}</main>}
