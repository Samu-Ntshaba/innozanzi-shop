import { unsubscribeNewsletter } from "@/domain/communications/actions";
export default async function UnsubscribePage({searchParams}:{searchParams:Promise<{email?:string;token?:string;done?:string}>}){const{email="",token="",done}=await searchParams;return <main className="mx-auto max-w-lg px-4 py-20 text-center">{done?<><h1 className="text-3xl font-bold">You’re unsubscribed</h1><p className="mt-3 text-slate-600">You will no longer receive marketing campaigns. Transactional account and service emails are unaffected.</p></>:<><h1 className="text-3xl font-bold">Unsubscribe from updates?</h1><p className="mt-3 text-slate-600">This stops marketing emails only.</p><form action={unsubscribeNewsletter} className="mt-6"><input type="hidden" name="email" value={email}/><input type="hidden" name="token" value={token}/><button className="rounded-lg bg-slate-900 px-5 py-3 font-semibold text-white" disabled={!email||!token}>Confirm unsubscribe</button></form></>}</main>}
