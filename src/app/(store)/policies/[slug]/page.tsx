import type { Metadata } from "next"; import { notFound } from "next/navigation"; import { prisma } from "@/lib/prisma";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const page=await prisma.page.findUnique({where:{slug:(await params).slug}});if(!page||page.status!=="PUBLISHED")return{};return{title:page.metaTitle??page.title,description:page.metaDescription,alternates:{canonical:`/policies/${page.slug}`}}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const page=await prisma.page.findUnique({where:{slug:(await params).slug}});if(!page||page.status!=="PUBLISHED")notFound();return <main className="mx-auto max-w-3xl px-4 py-12"><h1 className="text-3xl font-bold text-[#071b33]">{page.title}</h1><article className="mt-8 whitespace-pre-wrap leading-7 text-slate-700">{page.content}</article></main>}
