import type { Metadata } from "next";
import Link from "next/link";
import { ArrowRight, Gamepad2, Search, Wrench } from "lucide-react";
import { ProductCard } from "@/components/store/product-card";
import { getGamingCatalogue } from "@/domain/catalogue/queries";

export const dynamic="force-dynamic";
export const metadata:Metadata={title:"Gaming PCs, Components & Gear",description:"Shop gaming PCs, laptops, graphics cards, monitors, peripherals and streaming gear with live South African availability.",alternates:{canonical:"/gaming"}};

const href=(params:Record<string,string|undefined>)=>`/gaming?${new URLSearchParams(Object.entries(params).filter((entry):entry is [string,string]=>Boolean(entry[1]))).toString()}`;
export default async function GamingPage({searchParams}:{searchParams:Promise<{search?:string;group?:string;brand?:string;page?:string}>}){
  const query=await searchParams,data=await getGamingCatalogue({...query,page:Number(query.page)||1});
  return <main className="min-h-screen overflow-hidden bg-[#0b111b] text-white"><section className="border-b border-slate-800 bg-[#101824]"><div className="mx-auto grid max-w-7xl gap-8 px-4 py-12 sm:px-6 sm:py-16 lg:grid-cols-[1fr_340px] lg:px-8"><div><span className="inline-flex items-center gap-2 text-xs font-black uppercase tracking-[.18em] text-sky-300"><Gamepad2 className="size-4"/>Innozanzi Gaming</span><h1 className="mt-5 text-4xl font-black tracking-tight sm:text-6xl">Everything gaming.<br/>One place.</h1><p className="mt-5 max-w-2xl text-base leading-7 text-slate-300 sm:text-lg">Find gaming PCs, components and gear from our live catalogue.</p><form className="mt-7 flex max-w-2xl overflow-hidden rounded-xl border border-slate-600 bg-[#0b111b] focus-within:border-sky-400" action="/gaming"><Search className="ml-4 mt-4 size-5 text-slate-400"/><input name="search" defaultValue={query.search} placeholder="Search gaming gear" className="h-13 min-w-0 flex-1 bg-transparent px-3 outline-none"/><button className="m-1 rounded-lg bg-sky-500 px-5 text-sm font-black text-slate-950">Search</button></form></div><Link href="/build-a-pc" className="self-end rounded-2xl border border-slate-700 bg-[#0b111b] p-6 transition hover:border-sky-400"><Wrench className="size-8 text-sky-300"/><p className="mt-5 text-xs font-black uppercase tracking-widest text-slate-400">Custom machine</p><h2 className="mt-2 text-2xl font-black">Build your gaming PC</h2><span className="mt-5 inline-flex items-center gap-2 font-bold text-sky-300">Open PC workshop<ArrowRight className="size-4"/></span></Link></div></section><section className="mx-auto max-w-7xl px-4 py-7 sm:px-6 lg:px-8"><div className="flex gap-3 overflow-x-auto pb-3"><Link href={href({search:query.search,brand:query.brand})} className={`shrink-0 rounded-xl border px-4 py-3 text-sm font-bold ${!query.group?'border-sky-400 bg-sky-500 text-slate-950':'border-slate-700 bg-slate-900'}`}>All gaming <span className="ml-1 text-xs opacity-70">{data.total}</span></Link>{data.groups.map(group=><Link href={href({group:group.slug,search:query.search,brand:query.brand})} key={group.slug} className={`shrink-0 rounded-xl border px-4 py-3 text-sm font-bold transition hover:border-sky-400 ${query.group===group.slug?'border-sky-400 bg-sky-500 text-slate-950':'border-slate-700 bg-slate-900'}`}>{group.name}<span className="ml-2 text-xs opacity-60">{group.count}</span></Link>)}</div><div className="mt-5 flex flex-wrap items-center justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-widest text-sky-300">Gaming catalogue</p><h2 className="mt-1 text-2xl font-black">{data.total.toLocaleString("en-ZA")} products</h2></div><form action="/gaming" className="flex gap-2"><input type="hidden" name="group" value={query.group??""}/><input type="hidden" name="search" value={query.search??""}/><select name="brand" defaultValue={query.brand??""} className="h-11 rounded-xl border border-slate-700 bg-slate-900 px-3 text-sm" aria-label="Gaming brand"><option value="">All brands</option>{data.brands.map(brand=><option key={brand}>{brand}</option>)}</select><button className="rounded-xl border border-slate-700 px-4 text-sm font-bold">Apply</button></form></div>{data.products.length?<div className="mt-6 grid gap-4 min-[480px]:grid-cols-2 lg:grid-cols-4 [&_article]:border-slate-700 [&_article]:shadow-none">{data.products.map(product=><ProductCard product={product} key={product.id}/>)}</div>:<div className="mt-8 rounded-2xl border border-dashed border-slate-700 p-12 text-center text-slate-400">No gaming products match those filters. Try another category or search.</div>}{data.pages>1?<nav className="mt-8 flex justify-center gap-2" aria-label="Gaming catalogue pages">{Array.from({length:data.pages},(_,index)=>index+1).slice(Math.max(0,data.page-3),data.page+2).map(page=><Link className={`grid size-10 place-items-center rounded-lg border text-sm font-bold ${page===data.page?'border-sky-400 bg-sky-500 text-slate-950':'border-slate-700 bg-slate-900'}`} href={href({...query,page:String(page)})} key={page}>{page}</Link>)}</nav>:null}</section></main>;
}
