import Image from "next/image";
import Link from "next/link";
import { removeCartItemAction, updateCartItemAction } from "@/domain/cart/actions";
import { activeUnitPrice, calculateCart, getCurrentCart } from "@/domain/cart/service";
import { formatZar } from "@/lib/money";

export const dynamic = "force-dynamic";

export default async function CartPage({ searchParams }: { searchParams: Promise<{ error?: string; status?: string }> }) {
  const params = await searchParams;
  const cart = await getCurrentCart();
  const items = cart?.items ?? [];
  const totals = calculateCart(items);
  return <main className="mx-auto max-w-7xl px-4 py-10 sm:px-6 lg:px-8"><h1 className="text-4xl font-bold">Your cart</h1>{params.error && <p className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">That quantity is unavailable. Stock is checked before checkout.</p>}{params.status === "added" && <p className="mt-4 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-800">Product added to your cart.</p>}{items.length === 0 ? <div className="mt-8 rounded-2xl border border-dashed border-zinc-300 py-20 text-center"><h2 className="text-xl font-semibold">Your cart is empty</h2><Link className="mt-5 inline-block rounded-lg bg-emerald-700 px-5 py-3 font-semibold text-white" href="/shop">Browse products</Link></div> : <div className="mt-8 grid gap-8 lg:grid-cols-[1fr_360px]"><section className="space-y-4">{items.map((item) => { const image = item.product.images[0]; const price = activeUnitPrice(item.product, item.variant); return <article key={item.id} className="grid grid-cols-[96px_1fr] gap-4 rounded-2xl border border-zinc-200 p-4 sm:grid-cols-[112px_1fr_auto]"><div className="relative aspect-square rounded-xl bg-zinc-100">{image && <Image src={image.path} alt={item.product.name} fill sizes="112px" className="object-contain p-3" />}</div><div><Link className="font-semibold hover:text-emerald-800" href={`/products/${item.product.slug}`}>{item.product.name}</Link>{item.variant && <p className="mt-1 text-sm text-zinc-600">{item.variant.name}</p>}<p className="mt-2 font-bold">{formatZar(price.toString())}</p><div className="mt-3 flex gap-2"><form action={updateCartItemAction}><input type="hidden" name="itemId" value={item.id} /><input aria-label="Quantity" className="h-9 w-20 rounded-lg border border-zinc-300 px-2" name="quantity" type="number" min={1} max={99} defaultValue={item.quantity} /><button className="ml-2 text-sm underline" type="submit">Update</button></form><form action={removeCartItemAction}><input type="hidden" name="itemId" value={item.id} /><button className="text-sm text-red-700 underline" type="submit">Remove</button></form></div></div><p className="font-bold sm:text-right">{formatZar(price.mul(item.quantity).toString())}</p></article>; })}</section><aside className="h-fit rounded-2xl bg-zinc-950 p-6 text-white"><h2 className="text-xl font-bold">Order summary</h2><dl className="mt-6 space-y-3 text-sm"><div className="flex justify-between"><dt>Subtotal excluding VAT</dt><dd>{formatZar(totals.net.toString())}</dd></div><div className="flex justify-between"><dt>VAT (15%)</dt><dd>{formatZar(totals.vat.toString())}</dd></div><div className="flex justify-between border-t border-zinc-700 pt-4 text-lg font-bold"><dt>Total</dt><dd>{formatZar(totals.gross.toString())}</dd></div></dl><p className="mt-3 text-xs text-zinc-400">Delivery is calculated at checkout.</p><Link className="mt-6 block rounded-lg bg-amber-500 px-5 py-3 text-center font-semibold text-zinc-950" href="/checkout">Proceed to checkout</Link><Link className="mt-3 block text-center text-sm text-zinc-300 underline" href="/shop">Continue shopping</Link></aside></div>}</main>;
}
