import Image from "next/image";import Link from "next/link";import{removeCartItemAction,updateCartItemAction}from"@/domain/cart/actions";import{activeUnitPrice,calculateCart}from"@/domain/cart/calculations";import{getCurrentCart}from"@/domain/cart/service";import{formatZar}from"@/lib/money";import type{Metadata}from"next";
export const metadata:Metadata={title:"Your quotation list",robots:{index:false,follow:false}};
export const dynamic="force-dynamic";
export default async function CartPage({searchParams}:{searchParams:Promise<{error?:string;status?:string}>}){const params=await searchParams;const cart=await getCurrentCart();const items=cart?.items??[];const totals=calculateCart(items);return <main className="mx-auto max-w-7xl px-4 py-7 sm:px-6 sm:py-10 lg:px-8"><h1 className="text-3xl font-bold sm:text-4xl">Your quotation list</h1><p className="mt-2 text-slate-600">Review requested products and quantities. You are not purchasing or paying at this stage.</p>{params.error&&<p className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">That requested quantity is unavailable.</p>}{params.status==="added"&&<p className="mt-4 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-700">Product added to your quotation list.</p>}{!items.length?<div className="mt-8 rounded-2xl border border-dashed border-zinc-300 px-4 py-16 text-center"><h2 className="text-xl font-semibold">Your quotation list is empty</h2><Link className="mt-5 inline-block rounded-lg bg-sky-600 px-5 py-3 font-semibold text-white" href="/shop">Browse products</Link></div>:<div className="mt-7 grid gap-7 lg:grid-cols-[minmax(0,1fr)_360px]"><section className="min-w-0 space-y-4">{items.map(item=>{const image=item.product.images[0];const price=activeUnitPrice(item.product,item.variant);const inventory=item.variant?.inventory??item.product.inventory[0];const available=inventory?Math.max(0,inventory.onHand-inventory.reserved):0;return <article key={item.id} className="grid grid-cols-[80px_minmax(0,1fr)] gap-3 rounded-2xl border p-3 sm:grid-cols-[112px_minmax(0,1fr)_auto]"><div className="relative aspect-square rounded-xl bg-zinc-100">{image&&<Image src={image.path} alt={item.product.name} fill sizes="112px" className="object-contain p-2"/>}</div><div><Link className="font-semibold hover:text-sky-700" href={`/products/${item.product.slug}`}>{item.product.name}</Link><p className="mt-1 text-sm text-zinc-600">{item.variant?.name??item.product.sku} · {available} available</p><p className="mt-2 text-sm">Current catalogue price: <strong>{formatZar(price.toString())}</strong></p><div className="mt-3 flex flex-wrap gap-3"><form action={updateCartItemAction} className="flex items-center gap-2"><input type="hidden" name="itemId" value={item.id}/><input aria-label="Requested quantity" className="h-11 w-20 rounded-lg border px-2" name="quantity" type="number" min={1} max={available} defaultValue={item.quantity}/><button className="min-h-11 text-sm underline">Update quantity</button></form><form action={removeCartItemAction}><input type="hidden" name="itemId" value={item.id}/><button className="min-h-11 text-sm text-red-700 underline">Remove</button></form></div></div><p className="col-span-2 font-bold sm:col-span-1 sm:text-right">Requested: {item.quantity}</p></article>})}</section><aside className="h-fit rounded-2xl bg-zinc-950 p-6 text-white"><h2 className="text-xl font-bold">Indicative product value</h2><dl className="mt-6 space-y-3 text-sm"><div className="flex justify-between"><dt>Current subtotal</dt><dd>{formatZar(totals.net.toString())}</dd></div><div className="flex justify-between"><dt>Indicative VAT</dt><dd>{formatZar(totals.vat.toString())}</dd></div><div className="flex justify-between border-t border-zinc-700 pt-4 text-lg font-bold"><dt>Indicative total</dt><dd>{formatZar(totals.gross.toString())}</dd></div></dl><p className="mt-3 text-xs text-zinc-400">This is not a final price. Delivery, discounts and approved pricing are confirmed during quotation review.</p><Link className="mt-6 block min-h-12 rounded-lg bg-amber-500 px-5 py-3 text-center font-semibold text-zinc-950" href="/quotations/request">Continue quotation request</Link><Link className="mt-3 block py-3 text-center text-sm text-zinc-300 underline" href="/shop">Add more products</Link></aside></div>}</main>}
