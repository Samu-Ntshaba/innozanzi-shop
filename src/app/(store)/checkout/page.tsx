import { redirect } from "next/navigation";import { getCurrentCart } from "@/domain/cart/service";import { requireUser } from "@/domain/auth/session";import { calculateCart } from "@/domain/cart/calculations";import { formatZar } from "@/lib/money";import { placeRetailOrder } from "@/domain/checkout/actions";
export const dynamic="force-dynamic";
const input="mt-1 h-12 w-full rounded-lg border border-slate-300 px-3";
export default async function CheckoutPage(){await requireUser();const cart=await getCurrentCart();if(!cart?.items.length)redirect("/cart");const totals=calculateCart(cart.items);return <main className="mx-auto max-w-5xl px-4 py-10"><h1 className="text-3xl font-bold">Secure checkout</h1><p className="mt-2 text-slate-600">Confirm delivery and choose how you would like to pay.</p>{cart.supplierItems.length?<p className="mt-4 rounded-lg bg-amber-50 p-3 text-sm text-amber-900">Supplier-request items stay in your quotation workflow and are not included in this retail order.</p>:null}<form action={placeRetailOrder} className="mt-8 grid gap-8 lg:grid-cols-[1fr_360px]"><section className="grid gap-4 rounded-xl border p-6 sm:grid-cols-2"><h2 className="sm:col-span-2 text-xl font-bold">Delivery details</h2><label className="sm:col-span-2">Recipient<input className={input} name="recipient" required/></label><label>Phone<input className={input} name="phone" required/></label><label>Street address<input className={input} name="line1" required/></label><label>Address line 2<input className={input} name="line2"/></label><label>Suburb<input className={input} name="suburb"/></label><label>City<input className={input} name="city" required/></label><label>Province<input className={input} name="province" required/></label><label>Postal code<input className={input} name="postalCode" required/></label><label className="sm:col-span-2">Order notes<textarea className="mt-1 min-h-24 w-full rounded-lg border p-3" name="notes"/></label></section><aside className="h-fit rounded-xl bg-slate-50 p-6"><h2 className="text-xl font-bold">Order summary</h2>{cart.items.map(x=><div className="mt-3 flex justify-between gap-3 text-sm" key={x.id}><span>{x.quantity} × {x.product.name}</span><span>{formatZar(Number(x.product.regularPrice)*x.quantity)}</span></div>)}<dl className="mt-5 space-y-2 border-t pt-4"><div className="flex justify-between"><dt>Subtotal</dt><dd>{formatZar(totals.net)}</dd></div><div className="flex justify-between"><dt>VAT</dt><dd>{formatZar(totals.vat)}</dd></div><div className="flex justify-between text-lg font-bold"><dt>Total</dt><dd>{formatZar(totals.gross)}</dd></div></dl><fieldset className="mt-6 space-y-3"><legend className="font-bold">Payment</legend><label className="flex gap-3 rounded-lg border bg-white p-3"><input type="radio" name="paymentMethod" value="PAYSTACK" defaultChecked/> <span><strong>Paystack</strong><small className="block text-slate-500">Secure online card and supported payment methods</small></span></label><label className="flex gap-3 rounded-lg border bg-white p-3"><input type="radio" name="paymentMethod" value="EFT"/> <span><strong>Bank transfer (EFT)</strong><small className="block text-slate-500">Upload proof after placing the order</small></span></label></fieldset><button className="mt-6 w-full rounded-lg bg-sky-700 px-5 py-3 font-bold text-white">Place order</button><p className="mt-3 text-xs text-slate-500">Prices include VAT. Delivery charges, if required, will be confirmed before fulfilment.</p></aside></form></main>}
