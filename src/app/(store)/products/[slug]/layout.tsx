import { getProductBySlug } from "@/domain/catalogue/queries";
export default async function ProductLayout({children,params}:{children:React.ReactNode;params:Promise<{slug:string}>}){const product=await getProductBySlug((await params).slug);if(!product)return children;const image=product.images.find(i=>i.isPrimary)??product.images[0];const inventory=product.inventory[0];const available=inventory?inventory.onHand-inventory.reserved:0;const data={"@context":"https://schema.org","@type":"Product",name:product.name,sku:product.sku,description:product.shortDescription,image:image?.path?[image.path]:undefined,brand:product.brand?{"@type":"Brand",name:product.brand.name}:undefined,offers:{"@type":"Offer",priceCurrency:product.currency,price:(product.salePrice??product.regularPrice).toString(),availability:available>0?"https://schema.org/InStock":"https://schema.org/OutOfStock",url:`${process.env.NEXT_PUBLIC_SITE_URL??"https://shop.innozanzi.co.za"}/products/${product.slug}`}};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(data).replace(/</g,"\\u003c")}}/>{children}</>}
