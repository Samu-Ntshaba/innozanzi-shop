import { requireClientPortal } from "@/domain/client-portal/session";
import { prisma } from "@/lib/prisma";
export default async function PortalReturnsPage(){const{context}=await requireClientPortal();const cases=await prisma.returnCase.findMany({where:{customerId:context.user.id},orderBy:{createdAt:"desc"}});return <div className="mx-auto max-w-5xl"><p className="text-xs font-bold uppercase tracking-widest text-sky-700">After-sales service</p><h1 className="mt-1 text-3xl font-semibold">Returns and refunds</h1><p className="mt-2 text-sm text-slate-600">Track product assessments, resolutions and refund progress.</p><div className="mt-6 space-y-3">{cases.map(item=><div className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm" key={item.id}><div className="flex justify-between gap-3"><div><strong>{item.referenceNumber}</strong><p className="mt-1 text-sm text-slate-600">{item.productNameSnapshot} · Qty {item.quantityAffected}</p></div><span className="h-fit rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-800">{item.status.replaceAll("_"," ")}</span></div><p className="mt-3 text-xs text-slate-500">Resolution: {item.resolutionStatus.replaceAll("_"," ")} · Refund: {item.refundStatus.replaceAll("_"," ")}</p></div>)}{!cases.length?<div className="rounded-xl border border-dashed border-slate-300 bg-white p-10 text-center text-sm text-slate-500">No active return cases. Contact support if you need help with an order item.</div>:null}</div></div>}
