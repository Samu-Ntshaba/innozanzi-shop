import Link from "next/link";
import { requireClientPortal } from "@/domain/client-portal/session";
export default async function PortalTrainingPage(){await requireClientPortal();return <div className="mx-auto max-w-5xl"><p className="text-xs font-bold uppercase tracking-widest text-sky-700">Guidance</p><h1 className="mt-1 text-3xl font-semibold">Training and guidance</h1><p className="mt-2 text-sm text-slate-600">Practical help for your purchasing team.</p><div className="mt-6 grid gap-4 sm:grid-cols-2">{[{title:"Requesting the right product",text:"Include model numbers, required specifications, quantities and acceptable equivalents.",href:"/portal/requests/new"},{title:"Understanding order progress",text:"Use delivery tracking for shipment status, carrier information and expected dates.",href:"/portal/deliveries"},{title:"Need personal assistance?",text:"Open a support request and our team will guide you through the process.",href:"/portal/support"}].map(card=><Link className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm hover:border-sky-400" href={card.href} key={card.title}><h2 className="font-semibold">{card.title}</h2><p className="mt-2 text-sm leading-6 text-slate-600">{card.text}</p></Link>)}</div></div>}
