import{z}from"zod";
export const applicationDraftSchema=z.object({applicationId:z.string().uuid().optional(),partnershipTypeId:z.string().uuid(),currentStep:z.coerce.number().int().min(1).max(11).default(1),registeredBusinessName:z.string().trim().max(180).optional(),tradingName:z.string().trim().max(180).optional(),registrationNumber:z.string().trim().max(80).optional(),vatNumber:z.string().trim().max(80).optional(),businessAddress:z.string().trim().max(1000).optional(),representativeName:z.string().trim().max(160).optional(),representativeRole:z.string().trim().max(120).optional(),representativePhone:z.string().trim().max(40).optional(),businessProfile:z.string().trim().max(5000).optional(),productCategories:z.string().trim().max(1000).optional(),purchasingFrequency:z.string().trim().max(120).optional(),estimatedMonthlyValue:z.coerce.number().nonnegative().optional(),salesChannels:z.string().trim().max(1000).optional(),marketplaceLinks:z.string().trim().max(2000).optional(),references:z.string().trim().max(3000).optional()});
export const requestSchema=z.object({type:z.enum(["ECOMMERCE_SOURCING","BUSINESS_PROCUREMENT","RECURRING_SUPPLY","PROJECT","PRODUCT_RECOMMENDATION"]),urgency:z.enum(["NORMAL","URGENT","PROJECT"]),title:z.string().trim().min(4).max(180),description:z.string().trim().min(10).max(5000),sku:z.string().trim().max(100).optional(),quantity:z.coerce.number().int().positive().max(10000),marketplace:z.string().trim().max(150).optional(),targetPrice:z.coerce.number().positive().optional(),requiredMargin:z.coerce.number().min(0).max(100).optional(),deliveryLocation:z.string().trim().max(500).optional(),requiredDate:z.coerce.date().optional(),recurringSchedule:z.string().trim().max(300).optional(),customerNote:z.string().trim().max(2000).optional()});
