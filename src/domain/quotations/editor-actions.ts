"use server";
import Decimal from"decimal.js";import{revalidatePath}from"next/cache";import{z}from"zod";import{prisma}from"@/lib/prisma";import{requirePermission}from"@/domain/auth/session";import{priceFromCost}from"./lifecycle";
export async function addQuotationAlternative(formData:FormData){const ctx=await requirePermission("quotations.manage");const{quotationId,productId,quantity}=z.object({quotationId:z.string().uuid(),productId:z.string().uuid(),quantity:z.coerce.number().int().positive().max(10000)}).parse(Object.fromEntries(formData));await prisma.$transaction(async tx=>{const quote=await tx.quotation.findUniqueOrThrow({where:{id:quotationId}});if(quote.kind!=="PROVISIONAL")throw new Error("Final quotation items cannot be changed.");const product=await tx.product.findUniqueOrThrow({where:{id:productId},include:{suppliers:{where:{isPreferred:true},take:1}}});const cost=new Decimal(product.costPrice?.toString()??product.suppliers[0]?.costPrice.toString()??"");if(!cost.isFinite()||cost.lte(0))throw new Error("The alternative product has no verified cost price.");const price=priceFromCost(cost,quote.markupPercent,product.vatStatus==="TAXABLE");await tx.quotationItem.create({data:{quotationId,productId,productName:product.name,sku:product.sku,quantity,costPrice:cost,unitPrice:price.grossUnit,vatRate:product.vatStatus==="TAXABLE"?new Decimal("0.15"):new Decimal(0),vatTotal:price.vatUnit.mul(quantity),lineTotal:price.grossUnit.mul(quantity),notes:"Administrator-added alternative"}});const items=await tx.quotationItem.findMany({where:{quotationId}});const gross=items.reduce((sum,i)=>sum.plus(i.lineTotal),new Decimal(0));const vat=items.reduce((sum,i)=>sum.plus(i.vatTotal),new Decimal(0));await tx.quotation.update({where:{id:quotationId},data:{subtotal:gross.minus(vat),vatTotal:vat,grandTotal:gross.plus(quote.deliveryTotal).minus(quote.discountTotal),status:"UNDER_REVIEW"}});await tx.auditLog.create({data:{actorId:ctx.user.id,action:"quotation.item.add-alternative",entityType:"Quotation",entityId:quotationId,after:{productId,quantity}}})});revalidatePath(`/admin/quotations/${quotationId}`)}
export async function removeQuotationItem(formData:FormData){const ctx=await requirePermission("quotations.manage");const{quotationId,itemId}=z.object({quotationId:z.string().uuid(),itemId:z.string().uuid()}).parse(Object.fromEntries(formData));await prisma.$transaction(async tx=>{const quote=await tx.quotation.findUniqueOrThrow({where:{id:quotationId}});if(quote.kind!=="PROVISIONAL")throw new Error("Final quotation items cannot be changed.");if(await tx.quotationItem.count({where:{quotationId}})<=1)throw new Error("A quotation must retain at least one item.");await tx.quotationItem.delete({where:{id:itemId}});const items=await tx.quotationItem.findMany({where:{quotationId}});const gross=items.reduce((sum,i)=>sum.plus(i.lineTotal),new Decimal(0));const vat=items.reduce((sum,i)=>sum.plus(i.vatTotal),new Decimal(0));await tx.quotation.update({where:{id:quotationId},data:{subtotal:gross.minus(vat),vatTotal:vat,grandTotal:gross.plus(quote.deliveryTotal).minus(quote.discountTotal),status:"UNDER_REVIEW"}});await tx.auditLog.create({data:{actorId:ctx.user.id,action:"quotation.item.remove",entityType:"Quotation",entityId:quotationId,after:{itemId}}})});revalidatePath(`/admin/quotations/${quotationId}`)}
