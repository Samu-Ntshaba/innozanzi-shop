import Link from "next/link";
import { prisma } from "@/lib/prisma";
export async function OfferBanner(){const now=new Date();const offers=await prisma.coupon.findMany({where:{advertised:true,isActive:true,scope:"ORDER",AND:[{OR:[{startsAt:null},{startsAt:{lte:now}}]},{OR:[{expiresAt:null},{expiresAt:{gt:now}}]}]},include:{_count:{select:{redemptions:true}}},orderBy:{createdAt:"desc"},take:10});const offer=offers.find(o=>o.usageLimit===null||o._count.redemptions<o.usageLimit);if(!offer)return null;return <div className="border-b border-sky-100 bg-sky-50 px-4 py-2 text-center text-xs text-sky-950"><Link href="/shop" className="font-semibold">{offer.description||"Limited-time offer"} · {offer.automatic?"Automatic checkout promotion":`Code ${offer.code}`} · {offer.type==="PERCENTAGE"?`${offer.value}% off`:`R${offer.value} off`} eligible baskets</Link><span className="ml-2">{offer.minimumOrderValue?`Minimum R${offer.minimumOrderValue}. `:""}Exclusions and usage limits apply; confirmed at checkout.</span></div>}
