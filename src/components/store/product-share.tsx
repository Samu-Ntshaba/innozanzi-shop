"use client";
import { useState } from "react";
export function ProductShare({title,path}:{title:string;path:string}){const[copied,setCopied]=useState(false);const url=typeof window==="undefined"?`https://shop.innozanzi.co.za${path}`:new URL(path,window.location.origin).toString();const encoded=encodeURIComponent(url);const text=encodeURIComponent(`${title} from Innozanzi`);async function native(){if(navigator.share)await navigator.share({title,text:title,url});else{await navigator.clipboard.writeText(url);setCopied(true)}}return <section aria-label="Share this product" className="mt-6 border-t pt-5"><p className="text-xs font-bold uppercase tracking-wider text-slate-500">Share product</p><div className="mt-2 flex flex-wrap gap-2 text-sm"><a className="rounded border px-3 py-2 hover:border-sky-500" href={`https://wa.me/?text=${text}%20${encoded}`} target="_blank" rel="noreferrer">WhatsApp</a><a className="rounded border px-3 py-2 hover:border-sky-500" href={`https://www.facebook.com/sharer/sharer.php?u=${encoded}`} target="_blank" rel="noreferrer">Facebook</a><a className="rounded border px-3 py-2 hover:border-sky-500" href={`https://www.linkedin.com/sharing/share-offsite/?url=${encoded}`} target="_blank" rel="noreferrer">LinkedIn</a><a className="rounded border px-3 py-2 hover:border-sky-500" href={`https://twitter.com/intent/tweet?text=${text}&url=${encoded}`} target="_blank" rel="noreferrer">X</a><a className="rounded border px-3 py-2 hover:border-sky-500" href={`mailto:?subject=${text}&body=${encoded}`}>Email</a><button className="rounded border px-3 py-2 hover:border-sky-500" onClick={native} type="button">{copied?"Link copied":"Share / copy link"}</button></div></section>}
