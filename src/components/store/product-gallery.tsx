"use client";
import Image from"next/image";import{useState}from"react";
export function ProductGallery({images,name}:{images:string[];name:string}){const[active,setActive]=useState(0);return <div><div className="relative aspect-square overflow-hidden rounded-2xl border bg-white">{images[active]?<Image src={images[active]} alt={`${name} — image ${active+1}`} fill sizes="(max-width:1024px) 100vw,50vw" className="object-contain p-6" priority/>:<div className="grid h-full place-items-center text-slate-500">Product image unavailable</div>}</div>{images.length>1?<div className="mt-3 grid grid-cols-5 gap-2 sm:grid-cols-6">{images.slice(0,12).map((image,index)=><button aria-label={`View image ${index+1}`} aria-current={active===index} onClick={()=>setActive(index)} className={`relative aspect-square overflow-hidden rounded-lg border bg-white ${active===index?"ring-2 ring-sky-700":""}`} key={image}><Image src={image} alt="" fill sizes="100px" className="object-contain p-1"/></button>)}</div>:null}</div>}
