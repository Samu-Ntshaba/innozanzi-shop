{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "RAILPACK" },
  "deploy": {
    "startCommand": "node -e \"fetch(process.env.NEXT_PUBLIC_SITE_URL + '/api/cron/complete-delivered-orders',{method:'POST',headers:{Authorization:'Bearer ' + process.env.CRON_SECRET}}).then(async r=>{console.log(await r.text());if(!r.ok)process.exit(1)})\"",
    "cronSchedule": "15 * * * *",
    "restartPolicyType": "NEVER"
  }
}
