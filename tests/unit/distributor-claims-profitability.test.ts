import{describe,expect,it}from"vitest";import{assertClaimTransition,recoveryBalance}from"../../src/domain/returns/claim-rules";import{calculateReturnFinancials}from"../../src/domain/returns/profitability-rules";
describe("distributor recovery",()=>{it("enforces supplier response stages",()=>{expect(()=>assertClaimTransition("DRAFT","APPROVED")).toThrow("cannot move");expect(()=>assertClaimTransition("AWAITING_RESPONSE","APPROVED")).not.toThrow();expect(()=>assertClaimTransition("CLOSED","APPROVED")).toThrow()});it("prevents recovery exceeding the approved amount",()=>{expect(recoveryBalance(1000,250,500)).toBe(250);expect(()=>recoveryBalance(1000,900,101)).toThrow("exceeds");expect(()=>recoveryBalance(1000,0,0)).toThrow("positive")})});
describe("return profitability",()=>{it("keeps cost and recovery components separate",()=>{const x=calculateReturnFinancials({itemValue:1000,refundPaid:800,inspectionCost:50,repairCost:25,replacementCost:0,transportCost:100,distributorRecovery:500,resaleRecovery:75});expect(x.directCost).toBe(975);expect(x.totalRecovery).toBe(575);expect(x.netLoss).toBe(400);expect(x.retainedValue).toBe(600)})});
