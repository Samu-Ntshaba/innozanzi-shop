import { afterEach,beforeEach,expect,it,vi } from "vitest";
import { gatewayConfigured,hostedFields,ozowHash,payfastSignature,verifyApprovedNotification } from "@/integrations/payments/approved-gateways";
beforeEach(()=>{for(const[k,v]of Object.entries({OZOW_ENABLED:"true",OZOW_SITE_CODE:"site",OZOW_PRIVATE_KEY:"private-secret",OZOW_API_KEY:"api-secret",OZOW_TEST_MODE:"true",PAYFAST_ENABLED:"true",PAYFAST_MERCHANT_ID:"10000100",PAYFAST_MERCHANT_KEY:"merchant-secret",PAYFAST_PASSPHRASE:"pass phrase",PAYFAST_SANDBOX:"true"}))vi.stubEnv(k,v);});
afterEach(()=>{vi.unstubAllEnvs();vi.unstubAllGlobals();});
it("does not enable missing credentials or sandbox money in production",()=>{vi.stubEnv("PAYFAST_MERCHANT_KEY","");expect(gatewayConfigured("PAYFAST")).toBe(false);vi.stubEnv("NODE_ENV","production");expect(gatewayConfigured("OZOW")).toBe(false);});
it("creates signed provider forms with exact amount, owned reference and no private keys",()=>{for(const gateway of["OZOW","PAYFAST"] as const){const result=hostedFields(gateway,{id:"11111111-1111-4111-8111-111111111111",amount:"123.45",email:"test@example.com",orderId:"order"},"https://shop.example");expect(JSON.stringify(result)).not.toContain("private-secret");expect(JSON.stringify(result)).not.toContain("pass phrase");expect(Object.values(result.fields)).toContain("123.45");expect(result.url).toMatch(/^https:\/\/(sandbox.payfast.co.za|pay.ozow.com)/);}});
it("verifies PayFast signatures and server validation before accepting payment",async()=>{const data:Record<string,string>={m_payment_id:"payment",pf_payment_id:"gateway-event",payment_status:"COMPLETE",amount_gross:"123.45",amount_fee:"4.00",merchant_id:"10000100"};data.signature=payfastSignature(data,"pass phrase");vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true,text:async()=>"VALID"}));expect(await verifyApprovedNotification("PAYFAST",new URLSearchParams(data).toString())).toMatchObject({status:"PAID",amount:"123.45",currency:"ZAR",externalReference:"payment"});data.amount_gross="1.00";await expect(verifyApprovedNotification("PAYFAST",new URLSearchParams(data).toString())).rejects.toThrow("signature");});
it("rejects signed PayFast notifications when the provider refuses validation",async()=>{const data={merchant_id:"10000100",signature:""};data.signature=payfastSignature(data,"pass phrase");vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true,text:async()=>"INVALID"}));await expect(verifyApprovedNotification("PAYFAST",new URLSearchParams(data).toString())).rejects.toThrow("verified");});
it("validates Ozow hash and independently queries transaction status",async()=>{const data:Record<string,string>={SiteCode:"site",TransactionId:"event",TransactionReference:"payment",Amount:"123.45",Status:"Complete",Optional1:"",Optional2:"",Optional3:"",Optional4:"",Optional5:"",CurrencyCode:"ZAR",IsTest:"true",StatusMessage:""};data.Hash=ozowHash(Object.values(data),"private-secret");vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true,json:async()=>[{...data,Amount:123.45}]}));expect(await verifyApprovedNotification("OZOW",new URLSearchParams(data).toString())).toMatchObject({status:"PAID",currency:"ZAR",externalReference:"payment"});vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true,json:async()=>[{...data,Amount:1}]}));await expect(verifyApprovedNotification("OZOW",new URLSearchParams(data).toString())).rejects.toThrow("mismatch");});
it("rejects duplicate notification fields",async()=>{await expect(verifyApprovedNotification("OZOW","Amount=1&Amount=2")).rejects.toThrow("Duplicate");});
