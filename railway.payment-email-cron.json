{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "RAILPACK" },
  "deploy": {
    "startCommand": "npm run automation:emails",
    "cronSchedule": "*/10 * * * *",
    "restartPolicyType": "NEVER"
  }
}
